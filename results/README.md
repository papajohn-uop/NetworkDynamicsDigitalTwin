# Results Layout

This directory is organized by artifact type:

- `raw/` — run-level CSV and JSON outputs from the experiment harness
- `derived/` — summary CSVs and LaTeX table payloads used in the paper
- `figures/` — exported plots and figures

Keep raw experiment outputs in `raw/` and only promote finished artifacts into `derived/` or `figures/` once they are complete and publication-ready.